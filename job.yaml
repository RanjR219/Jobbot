apiVersion: batch/v1
kind: Job
metadata:
  name: jobbot-job
spec:
  template:
    spec:
      containers:
        - name: jobbot
          image: jobbot:latest
          imagePullPolicy: Never
      restartPolicy: Never
  backoffLimit: 2
